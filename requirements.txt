numpy>=1.24,<2.0
pandas>=2.0,<3.0
openpyxl>=3.1
flask>=3.0
# scikit-learn: installed separately without scipy to save ~100 MB disk space
# use: pip install scikit-learn --no-deps && pip install "joblib>=1.1.1" "threadpoolctl>=2.0.0"
scikit-learn>=1.3
